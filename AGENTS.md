# 서양 점성술 네이털 차트 — 개발 지침

## 작업 순서

1. 기능을 만들거나 변경하기 전 [프로젝트 명세](docs/PROJECT_SPEC.md)의 관련 절을 읽는다. 계산·해석 정의의 단일 기준은 이 명세다.
2. 계산 변경은 입력, 설정, 예상 결과, 허용오차를 갖춘 실패 재현 사례부터 작성한다. UI 변경은 실제 계산 결과와 표시의 일치를 확인한다.
3. 구현 후 관련 자동 검증과 별도 리뷰를 완료한다. 문서만 변경했다면 링크, 정의의 충돌, 요구사항 누락을 검토한다.
4. 완료 보고에는 변경 내용, 실제 수행한 검증, 남은 제약을 적는다. 문서 작성이나 라이브러리 설치를 차트 정확성 검증 완료로 표현하지 않는다.

## 제품 목표와 범위

- 생년월일시·출생지로 재현 가능한 서양 네이털 차트를 생성한다. 입력의 불확실성을 숨기지 않고 수치 결과와 해석을 추적 가능하게 제공한다.
- 기본 언어는 한국어이며 전문 용어는 영어를 병기한다. 중급~고급 해석과 서비스 설계를 지원한다.
- 계산 기준, 입력 처리, 필수 산출물: 명세 §2–7. 해석·출력: §8–10. 확장 기능: §11. 데이터·출시 검증: §12–14.
- 레퍼런스 사이트의 시각적 구성과 계산 설정을 따로 확인한다. 디자인이 비슷하다는 이유로 수치 정확성을 인정하지 않는다.

## 구현 시 지켜야 할 경계

- Swiss Ephemeris를 계산 코어에 연결한다. 패키지 이름만으로 적합성을 판단하지 말고 반환 플래그·오류·필요 데이터·지원 날짜를 검증한 adapter를 사용한다.
- 시간대 변환, 천문 계산, 점성술 규칙, 렌더링, 언어 해석을 분리한다. UI와 LLM은 검증된 결과를 소비하며 천체 위치를 생성하거나 보정하지 않는다.
- 엔진·천체력·시간대 데이터·규칙 버전을 고정한다. 동일 입력과 버전의 결과가 재현되어야 하므로 계산 시각 등의 운영 메타데이터는 결정적 결과 밖에 둔다.
- 계산 실패·묵시적 fallback·비정상 수치를 성공 결과로 내보내지 않는다. 명세의 상태와 오류 코드로 전달한다.
- 생시 미상·추정 시각의 결과는 해당 모드로 표시한다. 가정한 시각으로 산출한 ASC·MC·하우스를 실제 출생값으로 다루지 않는다.
- 새 기능은 규칙 정의와 양성·음성·경계 검증이 함께 완료되어야 활성화한다. 미구현 기능은 이용 가능한 것처럼 노출하지 않는다.
- 출생 원문을 로그·분석 이벤트·공유 URL에 담지 않는다. 저장·공유·외부 AI 해석의 데이터 흐름은 명세 §12를 따른다.

## 개발 시작 시 완료할 결정

명세에서 제안한 초기 범위를 바탕으로 아래를 기록하고 해당 기능 구현을 진행한다. 일상적인 기술 선택 때문에 사용자에게 반복 승인 요청을 하지 않는다.

- 검증된 engine adapter, 데이터 파일 manifest, 실행 환경과 동시성 격리 방식.
- 지원 기간·지역과 geocoding/timezone provider의 버전 및 역사 자료 검증 범위.
- 요청된 기능별 활성화 상태와 검증 사례. 아직 선택하지 않은 세부 규칙은 명세의 출시 조건을 충족한 뒤 활성화한다.
- 공개 서비스 활성화 전 엔진과 배포 데이터의 라이선스 선택·충족 여부. 유료 구매나 공개 배포는 별도 권한 범위에 따른다.

현재 저장소의 문서는 개발 기준이며, 차트 엔진이나 테스트가 구현·통과했다는 증거가 아니다.
